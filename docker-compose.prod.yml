services:
  nutri_web:
    build: .
    env_file:
      - .env.prod
    container_name: nutri_web
    command: ./start.sh
    volumes:
      - .:/app
      - ./backups:/app/db/backups
    ports:
      - "8001:8000"
    networks:
      - gateway-shared-net
      - rabbit-MQ-extern-network

volumes:
  nutri_dash_postgres_data:

networks:
  gateway-shared-net:
    external: true
  rabbit-MQ-extern-network:
    external: true
  
  